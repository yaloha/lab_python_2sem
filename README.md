# Лабораторная работа №2. Дескрипторы, свойства и инкапсуляция.

## Ключевые изменения от 1 лабораторной:
Реализована новая модель задачи (Task) (так же PydanticTask). Основное внимание уделено контролю доступа к атрибутам и защите внутреннего состояния модели.
- Реализованы Data Descriptors для автоматической валидации типов и их ограничений.
- Доступ к чувствительным данным (id, статус, дата создания) ограничен через свойства (`@property` getter).
- Защита через сеттеры: объект не позволяет установить некорректный статус (`@property` setter).
- Реализован дескриптор для форматированного вывода сводки по задаче (`summary`).
- Реализован дескриптор для форматированного вывода сводки по задаче (`summary`).
- Внедрена система кастомных исключений (TaskValidationError), которые выбрасываются при попытке создать или изменить задачу некорректными данными.
## Установка и запуск
1. **Инициализируйте venv, установите зависимости:**
   ```uv sync```
2. **Запустите проект**:
   - Приложение состоит из основного сервера и mock-сервера для API.
   - Запуск Mock-API (порт 8001): ```uv run python -m src.ext_backend```
   - Запуск основного сервера (порт 8000): ```uv run python -m src.main ```
3. **Запуск тестов**:
   - Запуск тестов: `pytest`
   - Запуск тестов с отчетом о покрытии: `pytest --cov=src`

